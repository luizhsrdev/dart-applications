name: bank_emulator_app
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: '>=3.7.0 <4.0.0'  # Certifique-se de incluir o intervalo correto

dependencies:
  http: ^1.3.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
